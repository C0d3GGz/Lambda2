type List<a> {
  Cons(a, List<a>),
  Nil()
}

let main: Int =
  letrec sum = \xs.
    match xs {
      List::Cons(h, t) => add h (sum t),
      List::Nil() => 0
    } in

    sum List::Cons(1, List::Nil());